<!-- CATEGORY CONTAINER -->
<div id="extruderLeft" class="{title:'Categories'}" style="display:none;">
  <div class="voice {}">
    <span class="label">
      <a class="label" href="#"></a>
    </span>
  </div>
  <!-- CATEGORY CONTENT -->
  <% @categories.each do |cate| %>
   <div class="voice {}">
    <span class="label">
      <a class="label" href="#" id="<%= cate.api_id%>_<%= cate.title %>">
        <%= cate.title %>
      </a>
    </span>
  </div>
  <% end %>

</div>
